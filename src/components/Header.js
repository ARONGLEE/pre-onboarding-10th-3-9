const Header = () => {
  return (
    // eslint-disable-next-line react/jsx-filename-extension
    <header className="header-container">
      <h1 className="header-title">Toodos</h1>
    </header>
  );
};

export default Header;
